{
  "name": "indie-frontend-tests",
  "private": true,
  "scripts": {
    "test": "playwright test --project=chromium --project=mobile",
    "test:layout": "playwright test --project=layout-chromium --project=layout-firefox --project=layout-webkit",
    "test:baseline": "playwright test --project=baseline"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
